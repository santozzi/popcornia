"use client";
import React from 'react';
import './Footer.css';
import PropTypes from 'prop-types';

const Footer = ({}) => {
	return (
		<div className='footer'>
 			Footer works!
 		</div>
	);
};

Footer.propTypes = {};

export default Footer;